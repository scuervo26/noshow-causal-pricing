# Python
.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Project-specific
# Raw dataset is retrievable from Kaggle (~11 MB) — fetch and place at the path below
datos/brutos/

# Processed data (regenerable from notebook 01) and output artefacts (regenerable from notebooks)
# Directories are created on demand by the modules in src/
datos/procesados/
outputs/

# Streamlit cache
.streamlit/

# Local env / secrets (none expected, but defensive)
.env
.env.local

# Planning / internal context docs — kept out of the public repo for now.
# Will be reintroduced (likely under docs/) once the project is finalised.
CLAUDE.md
PLAN_TECNICO.md
Registro_Propuesta_TFG.md
.claude/

# Reference library and written memoria — copyrighted PDFs / deposited document, not redistributable
referencias/
memoria/
